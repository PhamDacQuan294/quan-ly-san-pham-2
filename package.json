{
  "name": "quanlysanpham",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"nodemon index.ts\" \"npx sass --watch public/admin/scss:public/admin/css\" \"npx sass --watch public/client/scss:public/client/css\"",
    "build": "tsc && node copy-dir.js",
    "ts.check": "tsc --project tsconfig.json",
    "add-build": "git add dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": [
    "ts.check",
    "build",
    "add-build"
  ],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "pug": "^3.0.3",
    "sass": "^1.93.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^24.9.2",
    "concurrently": "^9.2.1",
    "fs-extra": "^11.3.2",
    "nodemon": "^3.1.10",
    "pre-commit": "^1.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
